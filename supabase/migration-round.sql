-- ============================================================
--  Migration: เพิ่มช่อง "งวดที่" (round_no) สำหรับทะเบียนรับเงินอุดหนุนรายงวด
--  วิธีใช้: เปิด Supabase > SQL Editor > New query > วางทั้งไฟล์นี้ > Run
--  ปลอดภัยที่จะรันซ้ำได้
-- ============================================================

alter table transactions add column if not exists round_no int;  -- งวดที่รับเงิน (1..สุดท้าย)
create index if not exists idx_txn_round on transactions(round_no);

-- ============================================================
--  เสร็จสิ้น — กลับไปที่เว็บแล้วกด "โหลดข้อมูลใหม่"
-- ============================================================
