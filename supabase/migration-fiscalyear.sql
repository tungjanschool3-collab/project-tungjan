-- ============================================================
--  Migration: แยกโครงการตามปีงบประมาณ
--  วิธีใช้: เปิด Supabase > SQL Editor > New query > วางทั้งไฟล์นี้ > Run
--  ปลอดภัยที่จะรันซ้ำได้
-- ============================================================

-- เพิ่มปีงบประมาณให้โครงการ (รายการรับ-จ่ายแยกปีจากวันที่อยู่แล้ว)
alter table projects add column if not exists fiscal_year int default 2569;
update projects set fiscal_year = 2569 where fiscal_year is null;
create index if not exists idx_projects_fy on projects(fiscal_year);

-- ============================================================
--  เสร็จสิ้น — กลับไปที่เว็บแล้วกด "โหลดข้อมูลใหม่"
-- ============================================================
